import './Fullscreen.css'
import db from './../data/db.json'
import SongBar from '../Player/SongBar/SongBar'

const Fullscreen = (props) => {
  return(
    <div id="fullscreen" onClick={props.fullscreenChange}>
      
    </div>
  )
}

export default Fullscreen